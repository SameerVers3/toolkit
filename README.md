# Personal tooling

personal tools. Right now there's one: a local Anthropic to Fireworks relay. More will land here over time.

## Tools

| Tool | Docs |
|------|------|
| Relay | [docs/relay.md](docs/relay.md) |

## Setup

```bash
python -m venv .venv
source .venv/bin/activate.fish   # bash: source .venv/bin/activate
pip install -e .
cp .env.example .env
```
